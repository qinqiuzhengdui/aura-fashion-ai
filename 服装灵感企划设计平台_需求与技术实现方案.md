# 服装灵感企划设计平台（Fashion AI Inspiration Studio）
## 详尽项目需求规范与全流程技术实现方案

---

## 1. 项目背景与产品定位

### 1.1 项目背景
在传统时尚行业中，服装企划与设计研发流程长达数周：
1. **资讯搜集耗时**：设计师与买手需跨多个平台（Vogue Runway、WGSN、Pinterest、Tagwalk、秀场资讯网）搜集海量灵感图，耗费数天时间手工分类与裁剪；
2. **概念传达损耗**：从模糊的风格构思（如“2026春夏 静奢风 垂坠感”）到具体的款式廓形、色彩板与面料定义，通常依赖口头沟通与多次试样，概念损耗大；
3. **商业提案排版繁琐**：设计师需手工整理图片、查找标准潘通色号、撰写面料与工艺特征，并使用设计软件排版成 PPT/PDF 提案，效率低下；
4. **生成式 AI 的落地瓶颈**：市面上普通的通用文生图工具缺乏时装专业领域理解，生成的服装结构常有畸变，同一企划下的多张图风格割裂，且无法输出工业打版所需的线稿与工艺说明。

### 1.2 产品核心定位
本项目旨在构建一个专为**服装设计师、品牌主理人、商品企划总监和时尚买手**量身打造的 **AI 驱动端到端时尚灵感企划工作站（Fashion AI Inspiration Studio）**。
系统以“智能体对话”为入口，通过“潮流资讯图库检索 -> 交互式筛选过滤 -> 多维度多视角协同生图 -> 企划特征深度提取（潘通色号/面料/工艺）-> 一键生成商业级 PPT 与交互式网页”的全流程闭环，将原先数天至数周的企划研发周期缩短至数分钟。

---

## 2. 现有方案不足之处与系统级完善策略

通过对初始需求与业务原型的深入推演，识别出 5 大关键技术瓶颈与业务不足，并制定了如下系统级完善措施：

```
+---------------------------------------------------------------------------------------------------+
|                                 核心瓶颈与完善方案对比表                                            |
+---------------------+---------------------------------------+-------------------------------------+
| 业务环节             | 初始设想潜在不足                      | 本方案完善与工程化升级策略           |
+---------------------+---------------------------------------+-------------------------------------+
| 1. 潮流资讯获取     | 实时爬虫易触发反爬/IP封禁，网络延迟高 | “离线向量图库 + 语义跨模态检索 +     |
|                     | 导致用户端卡顿，且有潜在商业版权风险   | 动态代理长尾补充”三层架构           |
+---------------------+---------------------------------------+-------------------------------------+
| 2. 多维度模型生图   | 纯文本生图随机性大、系列风格割裂，     | 引入 FLUX/SDXL + IP-Adapter 风格注入|
|                     | 人体结构易畸变，缺乏专业工艺线稿       | 与 ControlNet 结构约束，构建四维矩阵|
+---------------------+---------------------------------------+-------------------------------------+
| 3. 企划信息深度提炼 | 通用 LLM 对潘通色卡有严重幻觉，       | K-Means/CIELAB 图像主色聚类算法对齐 |
|                     | 面料成分与剪裁工艺描述泛泛而谈         | Pantone FHI TCX 权威色库 + 时尚 RAG |
+---------------------+---------------------------------------+-------------------------------------+
| 4. 交付物呈现与修改 | 静态 PPT 导出后不可二次微调，         | 动静双轨：Web 端沉浸式可编辑看板 +  |
|                     | 修改一个色号或图片需全盘推倒重来       | 基于现代时尚母版的 16:9 动态 PPTX   |
+---------------------+---------------------------------------+-------------------------------------+
| 5. 交互协同与体验   | 爬图与生图任务耗时较长，同步阻塞导致   | 全链路 SSE/WebSocket 流式分步通知， |
|                     | 前端超时，缺乏分步确认控制心智         | 渐进式渲染，支持“人机协同分步确认”  |
+---------------------+---------------------------------------+-------------------------------------+
```

---

## 3. 系统总体架构与技术栈选型

### 3.1 总体架构图
系统划分为五层：表现层、API 网关与业务编排层、Agent 智能体核心层、AI 模型与服务层、数据与存储层。

```mermaid
graph TD
    User([前端设计师用户]) --> WebUI[Web 交互客户端: React/Vue/Vanilla + Canvas]

    subgraph 接入与网关层
        WebUI --> Gateway[API Gateway / Nginx 反向代理]
        Gateway --> Auth[鉴权与租户管理]
        Gateway --> SSEStream[SSE / WebSocket 流式消息分发器]
    end

    subgraph 核心业务编排层
        SSEStream <--> Workflow[企划流程状态机 / Celery 异步任务流水线]
        Workflow --> LLMGateway[多大模型路由中间件 (Router)]
        Workflow --> SearchEngine[多模态图库检索服务]
        Workflow --> DiffusionEngine[多维度扩散生图引擎]
        Workflow --> AnalysisEngine[多模态特征提炼与潘通对齐引擎]
        Workflow --> ExportEngine[PPTX/Web 文档渲染引擎]
    end

    subgraph AI 模型与服务层
        LLMGateway --> Models[Claude 3.5 / GPT-4o / DeepSeek-V3 / Qwen2.5]
        SearchEngine --> VectorModel[CLIP / SigLIP 图像-文本跨模态嵌入模型]
        DiffusionEngine --> GenModels[FLUX.1 / SDXL / IP-Adapter / ControlNet]
        AnalysisEngine --> VLM[GPT-4o / Qwen2-VL 视觉分析 + OpenCV/Pillow 色谱分析]
    end

    subgraph 数据持久与检索层
        Workflow --> PG[(PostgreSQL 关系型主库)]
        Workflow --> Redis[(Redis 缓存 / 任务队列 / 会话状态)]
        SearchEngine --> Qdrant[(Qdrant / Milvus 向量数据库)]
        DiffusionEngine --> OSS[(MinIO / S3 对象存储 - 秀场图库与生成图)]
    end
```

### 3.2 核心技术栈选型推荐

1. **前端（Frontend）**：
   - **交互界面**：现代响应式单页架构（HTML5 + ES6 Modules + CSS3 变量系统 / Next.js React 18）。
   - **画板与交互**：Fabric.js / Konva.js（支持情绪板自由拖拽、缩放、裁剪构图）。
   - **文档预览与导出**：`pptxgenjs`（前端极速客户端生成 16:9 PPTX）、`html2canvas` + `jspdf`（高清 PDF 打印）。
2. **后端（Backend）**：
   - **服务框架**：Python FastAPI（异步高性能、天然支持多模态 AI 生态与科学计算库）。
   - **异步队列**：Celery + Redis（处理长耗时生图队列、图库爬取与分析任务）。
   - **多模型代理库**：LangChain / LiteLLM（统一 OpenAI 兼容接口，支持主流十余种 LLM 路由）。
3. **AI 基础设施（AI & Models）**：
   - **语言大模型**：OpenAI GPT-4o、Anthropic Claude 3.5 Sonnet、DeepSeek-V3、阿里通义千问 Qwen2.5-72B。
   - **多模态视觉大模型（VLM）**：GPT-4o Vision、Qwen2-VL-72B-Instruct。
   - **图像生成与可控扩散**：ComfyUI API / Replicate / 部署自建 FLUX.1-Dev、SDXL 1.0，配合 IP-Adapter-Plus（风格迁移）与 ControlNet（线稿/深度）。
   - **向量嵌入模型**：Google SigLIP 或 OpenAI CLIP-ViT-Large-Patch14。
4. **数据存储（Databases）**：
   - **关系数据库**：PostgreSQL 16（存储用户、企划工程、版本记录、元数据）。
   - **向量数据库**：Qdrant 或 Milvus（存储数百万张秀场图片的多模态特征向量）。
   - **对象存储**：AWS S3 或开源 MinIO（海量秀场原始大图与生成成果存储）。

---

## 4. 五大核心功能模块详尽技术规格

### 4.1 模块一：多大模型接入与智能对话中枢（LLM Gateway）
#### 功能描述
提供统一的智能体对话入口，支持用户自由选择或后台动态路由切换底层大模型（GPT-4o、Claude 3.5 Sonnet、DeepSeek-V3、Qwen 等），通过内置的**时尚企划专家 Prompt 模板系统**引导用户结构化表达设计构想。

#### 核心规格
1. **统一模型网关架构**：
   - 实现 LiteLLM 统一适配器规范，屏蔽各模型厂商的 API 格式差异；
   - 支持动态 Temperature、Top_p、Max_Tokens 调节；
   - 具备 Token 实时统计、接口自动重试与熔断降级机制（某模型超时自动切换至备选模型）。
2. **结构化灵感引导输入**：
   - **模式切换**：支持“企划案”、“服装设计”、“图案设计”三种业务场景；
   - **槽位填充引导**：品牌灵感源（如 ZIMMERMANN / LOUIS VUITTON）、年份季度（如 2026春夏）、设计品类组合（外套/衬衫/连衣裙/长裤）、性别/客群（女装/男装/童装）。
3. **会话流式响应**：
   - 前后端通过 Server-Sent Events (SSE) 协议实现打字机效果实时流式输出；
   - 支持历史会话存档与分支对话恢复。

---

### 4.2 模块二：潮流资讯图库采集与多模态检索系统（Trend Aggregator & Visual Search）
#### 功能描述
根据用户输入的主题构想与品牌要求（如“ZIMMERMANN 2026SS”、“LOUIS VUITTON 秀场”），系统通过接入**开放图像检索 API**（如 Unsplash API、Google Custom Search Image API）与**动态爬虫引擎**（如 Firecrawl / Playwright），自动上网实时搜索、抓取各大潮流资讯网站（Vogue Runway、Tagwalk、Pinterest、品牌官网）的最新服装大图。随后，多模态智能体（VLM Agent）根据用户的提示词要求进行二次精细化筛选，将最吻合主题的候选图片呈现给用户进行人机协同勾选。

#### 核心技术实现与 API 接入规范
1. **多源资讯 API 与动态爬虫接入网关**：
   - **开放时装 API（Unsplash / Pinterest API）**：通过 RESTful API 传输搜索关键词（如 `fashion runway Zimmerman resort 2026 dress`），毫秒级拉取官方高清大图与元数据；
   - **搜索引擎图像 API（Serper.dev / Google Image Search API）**：针对特定品牌在 Vogue Runway、Tagwalk 等网站上的网页进行精准图文抓取；
   - **轻量爬虫引擎（Firecrawl / Playwright Agent）**：当指定特定长尾品牌官网时，自动解析页面 DOM，提取高分辨率 HTML `<img src="..." alt="...">` 与 Lookbook 图片数据。
2. **多模态大模型智能筛选管线（VLM Prompt Filtering Pipeline）**：
   - **第一级（海选抓取）**：爬虫/API 搜集 50~100 张原始候选服装图片；
   - **第二级（VLM 匹配打分）**：将用户的提示词约束（如“短款外套、印花连衣裙、女装”）与候选图片序列批量输入多模态视觉大模型（GPT-4o Vision / Qwen2.5-VL）；
   - **匹配打分与过滤**：智能体输出结构化 JSON `{"image_id": "...", "relevance_score": 95, "matched_tags": ["短款外套", "柔和米色"]}`，自动剔除模特面部遮挡严重、背景杂乱或品类不符的脏数据，精选出 Top-15 高质量图片展示于 UI。
3. **用户筛选交互器（Curation Board）**：
   - 响应式瀑布流卡片，右上方提供复选框与单选/多选状态指示；
   - 交互功能：实时“已精选 X 张灵感图”计数、高保真灯箱放大预览、收藏及“换一批秀场图”（触发 API 重新搜索与重新筛选）。

---

### 4.3 模块三：多维度多视角协同图像生成流水线（Multi-Perspective Diffusion Pipeline）
#### 功能描述
单纯一张图片无法承载一个完整的服装系列。本模块基于用户选择的参考图与设计提示词，从 **4 个核心设计维度** 协同生成高审美、高保真的系列效果图。

#### 四大生成维度设计
1. **维度 A：设计主题氛围意向图（Moodboard & Atmosphere）**
   - **目标**：呈现系列的艺术内核、场景氛围、光影质感与情绪基调（如晨曦微光、静谧雕塑、自然肌理）。
   - **Prompt 策略**：注入 Cinematic lighting, haute couture ambiance, abstract fashion moodboard 等修饰词。
2. **维度 B：流行色搭配与面料肌理板（Color & Texture Board）**
   - **目标**：展示核心色彩比例搭配、潘通色块卡，以及核心面料的微距垂坠特写（如真丝乔其纱、立体刺绣蕾丝、提花棉麻纹理）。
   - **技术手段**：使用微距微观提示词与纹理质感 LoRA。
3. **维度 C：关键款式秀场全身造型（Runway Key Silhouettes / Lookbook）**
   - **目标**：生成全身穿搭的模特走秀效果，展现整体廓形（H型/A型/沙漏型/茧型）及叠穿搭配效果。
4. **维度 D：服装款式白底平铺/工艺线稿图（Flat Sketches & Tech Pack）**
   - **目标**：输出打版打样所需的服装平铺正反面结构图、纽扣、口袋及车缝线细节。
   - **技术手段**：引入 ControlNet Lineart / Canny 边缘检测，生成白底高精矢量矢量化款式图。

#### 风格一致性保障技术
- **IP-Adapter 风格注入**：将用户在模块二中筛选的图片输入 IP-Adapter 图像编码器，提取风格向量并注入扩散模型的 UNet/DiT 交叉注意力层，确保生成的服装款式在色彩体系、面料反光和美学调性上与选定图严格一致。
- **Seed 锁定与 LoRA 融合**：使用统一的基础 Seed 偏移机制，搭配轻量级高级时装 LoRA 模型（如 Quiet Luxury 专属微调模型）。

---

### 4.4 模块四：多模态企划特征深度提取与专业精炼（Feature Extraction & Refinement）
#### 功能描述
调用多模态视觉大语言模型（VLM）与图像色彩分析算法，对用户选用的参考图进行深度解析，提取出符合服装工业化标准的结构化企划数据。

#### 核心提取维度与算法方案
1. **色彩特征与标准潘通色号自动匹配（Pantone Matching）**：
   - **算法流水线**：
     $$\text{选定图片} \xrightarrow{\text{去除背景/模特肤色掩膜}} \text{K-Means 聚类 (k=5)} \xrightarrow{\text{提取 RGB/HEX}} \xrightarrow{\text{转换至 CIELAB 空间}} \text{计算 } \Delta E_{00} \text{ 最小色差} \xrightarrow{\text{对齐 Pantone FHI TCX 数据库}}$$
   - **色彩分级输出**：
     - **主推色（Key Color）**：如 *柔和米色 (Pantone 13-1008 TCX Oat Milk)*，附流行色趋势分析与心理学定位；
     - **辅色（Secondary Colors）**：如 *珍珠白 (Pantone 11-0604 TCX Coconut Milk)*、*浅银灰 (Pantone 14-4202 TCX Quiet Shade)*；
     - **基础色（Base Colors）**：如 *本白 (Pantone 11-0602 TCX White Alyssum)*、*墨黑 (Pantone 19-3900 TCX True Black)*。
2. **面料特征提取（Fabric & Material Specs）**：
   - 识别材质品类（如薄纱/网纱、刺绣蕾丝、真丝乔其纱、提花棉麻混纺）；
   - 输出组织结构、触感特征、悬垂度、透明度及推荐应用部位。
3. **服装设计与剪裁工艺提取（Silhouette & Craftsmanship）**：
   - 提取设计亮点：立体花卉缝制、珠饰亮片点缀、抽褶、解构下摆、流线型垂坠；
   - 提炼适用客群、设计适用场景（晚宴、度假、都市通勤）。
4. **人工在轨校验（Human-in-the-loop）**：
   - 提取结果以结构化卡片呈现，允许设计师在线点击修改色号、调整面料描述或补充特殊工艺备注。

---

### 4.5 模块五：跨模态企划文档排版与多端导出引擎（Multi-Format Document Engine）
#### 功能描述
将提炼的结构化文案、提取的潘通色板、筛选的参考图以及多维度生成的 AI 图像无缝编排，输出为**交互式 Web 看板**和**商业级 16:9 PPTX 演示文稿**。

#### 导出格式与规格
1. **Web 端沉浸式企划演示看板（Interactive Digital Deck）**：
   - 暗黑系/高级灰轻奢视觉风格，与时装周大片调性完美契合；
   - 支持交互式灯箱、色块 HEX 值一键复制、图片一键重新生成；
   - 支持自适应响应式布局（手机端移动买手评审、大屏投影演示）。
2. **商业提案 PPTX 导出（16:9 宽屏版式）**：
   - 基于精细调校的时尚设计母版生成，非简单的截图拼接，而是原生的图文矢量混排：
     - **Slide 1**：企划封面（项目名称、灵感主题、年份季节、主理人/设计师）；
     - **Slide 2**：设计灵感与概念故事（Mood & Storyboard、参考图矩阵）；
     - **Slide 3**：流行色与色彩搭配矩阵（色彩占比饼图、Pantone TCX 矢量色块卡、中英文色号标注）；
     - **Slide 4**：面料与工艺说明（微距肌理图、材质特性与打板要求）；
     - **Slide 5**：关键造型秀场效果图（系列全身 Lookbook）；
     - **Slide 6**：单品款式平铺与工艺分解（白底线稿、工艺单说明）。
3. **单文件离线 HTML / PDF 打印包**：
   - 支持将整个企划看板打包为包含 Base64 图片的单个离线 HTML 文件，或导出为 300DPI 高清印刷级 PDF 归档。

---

## 5. 核心数据模型设计（PostgreSQL Schema）

```sql
-- 1. 企划项目主表
CREATE TABLE projects (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    title VARCHAR(255) NOT NULL,
    season VARCHAR(64) NOT NULL,         -- e.g. "2026春夏"
    brand_reference VARCHAR(128),       -- e.g. "LOUIS VUITTON / ZIMMERMANN"
    target_gender VARCHAR(32),          -- "女装" / "男装" / "童装"
    target_categories TEXT[],           -- ARRAY["短款外套", "衬衫", "连衣裙"]
    status VARCHAR(32) DEFAULT 'draft', -- 'draft', 'curating', 'analyzing', 'completed'
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 2. 灵感参考图片库表（爬取与索引）
CREATE TABLE inspiration_images (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    source_url TEXT NOT NULL,
    storage_path TEXT NOT NULL,
    brand VARCHAR(128),
    season VARCHAR(64),
    category VARCHAR(64),
    tags TEXT[],
    embedding VECTOR(1152),             -- SigLIP 多模态特征向量
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 3. 用户企划选定参考图关联表
CREATE TABLE project_selected_images (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    image_id UUID REFERENCES inspiration_images(id),
    is_primary BOOLEAN DEFAULT FALSE,
    sort_order INT DEFAULT 0
);

-- 4. 企划深度提取特征表
CREATE TABLE project_specifications (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID UNIQUE REFERENCES projects(id) ON DELETE CASCADE,
    color_palette JSONB NOT NULL,       -- 包含主题色、副色、基础色及其 Pantone TCX 色号
    fabric_features JSONB NOT NULL,     -- 包含材质名称、特性、适用部位
    design_features JSONB NOT NULL,     -- 剪裁廓形、立体花卉、特殊工艺
    concept_summary TEXT,               -- AI 提炼的设计主张与灵感故事
    raw_vlm_response JSONB,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 5. AI 多维度生成资产表
CREATE TABLE generated_assets (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    dimension VARCHAR(64) NOT NULL,     -- 'theme_mood', 'color_texture', 'key_silhouette', 'flat_sketch'
    prompt TEXT NOT NULL,
    storage_path TEXT NOT NULL,
    thumbnail_path TEXT,
    aspect_ratio VARCHAR(16) DEFAULT '3:4',
    seed BIGINT,
    parameters JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

---

## 6. 核心接口定义与通信协议规范

### 6.1 核心 RESTful API
- `POST /api/v1/projects`：创建新企划，初始化状态。
- `POST /api/v1/trend/search`：根据输入 Prompt 执行多模态向量混合检索，返回候选秀场参考图列表。
- `POST /api/v1/projects/{id}/curate`：提交用户勾选的精选图片 ID 列表。
- `POST /api/v1/projects/{id}/analyze`：对选定图片触发 VLM 多模态提取与潘通色号自动匹配。
- `POST /api/v1/projects/{id}/generate`：提交多维度生图任务，推入 Celery 任务队列。
- `GET /api/v1/projects/{id}/export?format=pptx|web|pdf`：下载商业提案 PPTX 或生成网页离线包。

### 6.2 SSE 实时流式事件协议（`/api/v1/projects/{id}/events`）
客户端通过单向长连接订阅任务进度：
```json
// 事件 1: 图库检索完成
event: trend_retrieved
data: {"count": 15, "task_id": "t-101"}

// 事件 2: 正在分析色彩与面料
event: analyzing_features
data: {"step": "color_clustering", "progress": 40}

// 事件 3: 特征提取完成
event: features_extracted
data: {"pantone_matched": 6, "fabrics_found": 4}

// 事件 4: 多维度图像逐步生成推送
event: asset_generated
data: {"dimension": "theme_mood", "image_url": "/assets/gen/mood_01.webp"}
event: asset_generated
data: {"dimension": "color_texture", "image_url": "/assets/gen/palette_01.webp"}
event: asset_generated
data: {"dimension": "key_silhouette", "image_url": "/assets/gen/look_01.webp"}

// 事件 5: 全部任务就绪
event: project_ready
data: {"export_available": true}
```

---

## 7. 关键算法与代码实现核心逻辑

### 7.1 图片色彩聚类与标准 Pantone TCX 色号映射（Python 实现）
```python
import numpy as np
from PIL import Image
from sklearn.cluster import KMeans
from colormath.color_objects import sRGBColor, LabColor
from colormath.color_conversions import convert_color
from colormath.color_diff import delta_e_cie2000

# 权威 Pantone FHI (TCX) 标准棉布版精选子集库
PANTONE_TCX_DB = [
    {"code": "13-1008 TCX", "name": "Oat Milk", "hex": "#E6DBC9"},
    {"code": "11-0604 TCX", "name": "Coconut Milk", "hex": "#F0EFEA"},
    {"code": "14-4202 TCX", "name": "Quiet Shade", "hex": "#A0A2A3"},
    {"code": "11-0602 TCX", "name": "White Alyssum", "hex": "#F2F0EB"},
    {"code": "15-1304 TCX", "name": "Moonbeam", "hex": "#CDC6BD"},
    {"code": "19-3900 TCX", "name": "True Black", "hex": "#1E1F21"}
]

def hex_to_lab(hex_str: str) -> LabColor:
    hex_clean = hex_str.lstrip('#')
    r, g, b = [int(hex_clean[i:i+2], 16) / 255.0 for i in (0, 2, 4)]
    rgb = sRGBColor(r, g, b)
    return convert_color(rgb, LabColor)

def extract_dominant_colors(image_path: str, n_colors=5):
    """通过 K-Means 聚类提取图像最具代表性的主色调"""
    image = Image.open(image_path).convert('RGB')
    image = image.resize((150, 150))
    pixels = np.array(image).reshape(-1, 3)
    
    kmeans = KMeans(n_clusters=n_colors, random_state=42, n_init='auto')
    kmeans.fit(pixels)
    colors = kmeans.cluster_centers_.astype(int)
    
    hex_colors = [f"#{c[0]:02x}{c[1]:02x}{c[2]:02x}".upper() for c in colors]
    return hex_colors

def match_pantone_tcx(target_hex: str):
    """基于 CIELAB Delta E 2000 颜色感知差异算法，检索最接近的 Pantone TCX 色号"""
    target_lab = hex_to_lab(target_hex)
    best_match = None
    min_delta = float('inf')
    
    for item in PANTONE_TCX_DB:
        cand_lab = hex_to_lab(item["hex"])
        delta = delta_e_cie2000(target_lab, cand_lab)
        if delta < min_delta:
            min_delta = delta
            best_match = item
            
    return {
        "original_hex": target_hex,
        "pantone_code": best_match["code"],
        "pantone_name": best_match["name"],
        "standard_hex": best_match["hex"],
        "delta_e": round(min_delta, 2)
    }
```

### 7.2 商业级 16:9 PPTX 动态排版引擎（`python-pptx` 核心逻辑）
```python
from pptx import Presentation
from pptx.util import Inches, Pt
from pptx.dml.color import RGBColor
from pptx.enum.text import PP_ALIGN

def create_fashion_proposal_deck(project_data: dict, output_path: str):
    prs = Presentation()
    # 强制设置 16:9 现代宽屏比例
    prs.slide_width = Inches(13.333)
    prs.slide_height = Inches(7.5)
    blank_layout = prs.slide_layouts[6]
    
    # 1. 封面幻灯片
    slide = prs.slides.add_slide(blank_layout)
    bg = slide.background
    fill = bg.fill
    fill.solid()
    fill.fore_color.rgb = RGBColor(18, 20, 24) # 时尚暗黑背景
    
    # 添加主副标题
    txBox = slide.shapes.add_textbox(Inches(1.5), Inches(2.5), Inches(10), Inches(2))
    tf = txBox.text_frame
    p1 = tf.paragraphs[0]
    p1.text = project_data.get("title", "2026春夏 女装设计企划案")
    p1.font.size = Pt(44)
    p1.font.bold = True
    p1.font.color.rgb = RGBColor(245, 245, 247)
    
    p2 = tf.add_paragraph()
    p2.text = f"灵感来源：{project_data.get('brand')} | 主题：{project_data.get('concept_theme')}"
    p2.font.size = Pt(20)
    p2.font.color.rgb = RGBColor(160, 165, 175)
    
    # 2. 色彩矩阵幻灯片 (带 Pantone 色块卡)
    slide2 = prs.slides.add_slide(blank_layout)
    slide2.background.fill.solid()
    slide2.background.fill.fore_color.rgb = RGBColor(18, 20, 24)
    
    title_box = slide2.shapes.add_textbox(Inches(1), Inches(0.8), Inches(8), Inches(1))
    title_box.text_frame.paragraphs[0].text = "COLOR PALETTE / 流行色体系与潘通标准色卡"
    title_box.text_frame.paragraphs[0].font.size = Pt(24)
    title_box.text_frame.paragraphs[0].font.color.rgb = RGBColor(255, 255, 255)
    
    # 循环绘制 5 个潘通色卡矢量矩形
    colors = project_data.get("colors", [])
    for idx, col in enumerate(colors):
        left = Inches(1 + idx * 2.3)
        top = Inches(2.2)
        # 色块
        shape = slide2.shapes.add_shape(1, left, top, Inches(2), Inches(2.4))
        r, g, b = [int(col['hex'][i:i+2], 16) for i in (1, 3, 5)]
        shape.fill.solid()
        shape.fill.fore_color.rgb = RGBColor(r, g, b)
        shape.line.fill.background()
        
        # 标签说明
        label_box = slide2.shapes.add_textbox(left, Inches(4.8), Inches(2), Inches(1.5))
        ltf = label_box.text_frame
        lp1 = ltf.paragraphs[0]
        lp1.text = col['pantone_code']
        lp1.font.size = Pt(12)
        lp1.font.bold = True
        lp1.font.color.rgb = RGBColor(220, 220, 225)
        
        lp2 = ltf.add_paragraph()
        lp2.text = col['name']
        lp2.font.size = Pt(10)
        lp2.font.color.rgb = RGBColor(160, 160, 165)
        
    prs.save(output_path)
```

---

## 8. 实施路径与演进路线图

1. **第一阶段：轻量高保真交互 Demo 原型开发（当前阶段）**
   - 实现高保真 Web 客户端，完整复现 `需求文档.docx` 中 5 张交互截图的全部核心逻辑；
   - 接入模拟/真实大模型对话、秀场多图筛选瀑布流、多维度生图预览、Pantone TCX 色卡计算器与 PPTX 导出。
2. **第二阶段：后端与向量索引流水线搭建**
   - 部署 FastAPI + Qdrant 向量检索服务，清洗入库首批 50,000+ 张秀场高清图；
   - 统一 OpenAI / Claude / DeepSeek 大模型路由网关。
3. **第三阶段：ComfyUI / FLUX.1 多维度生图集群接入**
   - 部署 IP-Adapter 与 ControlNet 自动化生图工作流，支持一键高保真生成款式图。
4. **第四阶段：生产化企业级部署与商用合规**
   - 完善数据安全隔离、API 鉴权与流控、图库版权保护与水印机制。
